This is the command required to download a Repository created on github website on your local env:

git clone git@github.com:your_username/repo_name.git local_folder

For example, to download the repo GitCommands into a local folder called LocalGitCommands, this is the command:
git clone git@github.com:22dt91/GitCommands.git LocalGitCommands
